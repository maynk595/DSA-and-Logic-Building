#include<iostream>
using namespace std;
int main(){
    int a,b;
    cin>>a>>b;
    cout<<(a>b ? a : b)<<endl; // ternary operator (if condition is true then it will return first value otherwise second value)
    return 0;
}